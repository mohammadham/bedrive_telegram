- [ ] **بخش تنظیمات تلگرام کاربر:**
    - [ ] ایجاد یک کامپوننت React جدید برای صفحه تنظیمات تلگرام کاربر.
    - [ ] افزودن یک آیتم منوی جدید در نوار کناری تنظیمات کاربر برای دسترسی به این صفحه.
    - [ ] پیاده‌سازی فرم برای وارد کردن `telegram_chat_id` و فعال/غیرفعال کردن `auto_send_to_telegram`.
    - [ ] اتصال کامپوننت به API endpoints برای دریافت و ذخیره تنظیمات.
- [ ] **ادغام درایور ذخیره‌سازی تلگرام:**
    - [ ] ثبت `TelegramStorageServiceProvider` در `config/app.php`.
    - [ ] افزودن یک دیسک جدید به نام `telegram` در `config/filesystems.php`.
    - [ ] پیاده‌سازی منطق لازم برای استفاده از این دیسک هنگام آپلود فایل‌ها در صورت فعال بودن ذخیره‌سازی تلگرام.
- [ ] **بهبود رابط کاربری مدیریت تلگرام در پنل ادمین:**
    - [ ] نمایش وضعیت فعلی اتصال به تلگرام (متصل/غیرمتصل).
    - [ ] ارائه بازخورد بهتر هنگام پیکربندی session (مثلاً نمایش درخواست کد تأیید).
    - [ ] افزودن دکمه‌ای برای حذف session و پیکربندی مجدد.
- [ ] **تکمیل و بهبود تست‌ها:**
    - [ ] نوشتن تست‌های Feature برای بخش تنظیمات تلگرام کاربر.
    - [ ] بهبود تست‌های موجود برای پوشش موارد بیشتر (مانند آپلود ناموفق).
    - [ ] نوشتن تست‌های واحد برای `TelegramStorageDriver`.
- [ ] **ایجاد GitHub Actions برای CI/CD:**
    - [ ] ایجاد یک workflow برای اجرای خودکار تست‌ها (`php artisan test`).
    - [ ] ایجاد یک workflow برای ساخت پروژه (build) و ایجاد یک فایل فشرده به عنوان خروجی.
    - [ ] افزودن مرحله‌ای برای نصب وابستگی‌های پایتون (`pip install telegram-upload`).
- [ ] **مستندسازی:**
    - [ ] به‌روزرسانی `README.md` با دستورالعمل‌های دقیق‌تر برای پیکربندی و استفاده از ویژگی‌های جدید.
    - [ ] مستندسازی API endpoints جدید در یک فایل جداگانه (مانند `API.md`).
